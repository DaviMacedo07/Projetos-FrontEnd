let A = Number(lines.shift("Digite um Número"))
let B = Number(lines.shift("Digite um Número"))
let C = Number(lines.shift("Digite um Número"))
let D = Number(lines.shift("Digite um Número"))

let DIFERENCA = ((A * B) - (C * D))

console.log(`DIFERECA = ${DIFERENCA}`)